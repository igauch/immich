# 修改说明

## 原则和设计目标
1. 扫描和上传场景统一
2. 时间优先、时间自动更正
3. 数据安全

## 添加 fileHash
虽然已经存在checksum，但是checksum在扫描场景是文件路径的hash，不同于上传时的文件内容hash，新增fileHash就是明确标记文件内容的hash

todo: 扫描时文件就是故意重复呢？

## 添加 realCreateTime
因为很多不可控和不明确的场景导致我们在处理文件时很容易就修改了文件的创建时间，哪怕只是移动或者转存等。但创建时间是极其重要的，尤其是对照片来讲，所以保证创建时间尽可能正确是很有意义的。

## 添加加解密文件以备份到云端功能
备份是必要的，云上的数据加密也是必要的
